name: HermesiOS
options:
  bundleIdPrefix: com.hermeswebui
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
    ENABLE_USER_SCRIPT_SANDBOXING: NO
    SWIFT_STRICT_CONCURRENCY: minimal

packages:
  HermesiOSLib:
    path: .

targets:
  HermesiOS:
    type: application
    platform: iOS
    sources:
      - path: Sources/HermesApp
      - path: Sources/HermesApp/PrivacyInfo.xcprivacy
        buildPhase: resources
    dependencies:
      - package: HermesiOSLib
        product: HermesApp
      - package: HermesiOSLib
        product: HermesWebView
      - package: HermesiOSLib
        product: HermesCapabilities
      - package: HermesiOSLib
        product: HermesUI
      - package: HermesiOSLib
        product: HermesCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hermeswebui.HermesiOS
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
    info:
      path: Sources/HermesApp/Info.plist
      properties:
        CFBundleDisplayName: Hermes
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        LSRequiresIPhoneOS: true
        # Encryption export classification — we use standard TLS only (URLSession + WKWebView),
        # which is exempt under "uses or accesses only exempt cryptography." Declaring false here
        # suppresses the TestFlight encryption-classification prompt on every upload.
        ITSAppUsesNonExemptEncryption: false
        UILaunchScreen:
          UIColorName: ""
        # Permission strings — only what real flows actually exercise.
        NSCameraUsageDescription: "Hermes uses the camera to scan the QR code that connects you to your Hermes Agent."
        NSFaceIDUsageDescription: "Hermes uses Face ID to authorize sensitive agent actions on this device."
        # ATS: allow non-HTTPS in WebView content for LAN dev (Tailscale magic-DNS hostnames are HTTPS).
        # Native networking is unaffected.
        NSAppTransportSecurity:
          NSAllowsArbitraryLoadsInWebContent: true
        # Custom URL scheme for endpoint-share fallback (hermes://agent?payload=<base64>).
        CFBundleURLTypes:
          - CFBundleURLName: com.hermeswebui.HermesiOS.endpoint
            CFBundleURLSchemes:
              - hermes

  HermesiOSTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Tests/HermesiOSTests
    dependencies:
      - target: HermesiOS
      - package: HermesiOSLib
        product: HermesCore
      - package: HermesiOSLib
        product: HermesCapabilities
